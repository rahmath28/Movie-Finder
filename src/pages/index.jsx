import MovieDetails from "./MovieDetails";
import MovieList from "./MovieList";
import Search from "./Search";
import PageNotFound from "./PageNotFound";

// // imported all the page components here , and exporting as object.
// // index.jsx doesnt act like component, because start with small letter.
// // REamaing ellam oru component mari than act agum , kept in a pages folder.

export {MovieDetails, MovieList, Search, PageNotFound };
// exported , then set route to each component.

